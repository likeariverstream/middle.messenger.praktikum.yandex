@import url("../../vars.pcss");

.message {
    max-width: 240px;
    border-radius: 4px;
    display: flex;
    padding: 4px;
    align-self: flex-start;
    background: $backgroundGray;
}

.message-mine {
    align-self: flex-end;
}